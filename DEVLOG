
6-16-26
  
 First program is simple using:

x = (Length1 * cos(theta_1))  + (Length2 * cos(theta_1 + theta_2));

y = (Length1 * sin(theta_1))  + (Length2 * sin(theta_1 + theta_2));

to calculate the positon of the end affector

  lengths and theta value are inputted by user. Theta is converted from degrees to radians before entering the function.





  
